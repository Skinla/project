<?php
// config.php

return [
    // Папка для файлов очереди
    'queue_dir' => __DIR__ . '/queue',

    // Папка для лог-файлов
    'logs_dir' => __DIR__ . '/logs',

    // Максимальный размер лога
    'max_log_size' => 2 * 1024 * 1024, // 2 МБ

    // Основной лог-файл
    'global_log' => 'global.log',

    // Управление детальным логированием (false = только критические ошибки, ускоряет работу)
    'enable_detailed_logging' => false, // Установите true для полного логирования при отладке

    // Ответственный (ID)
    'assigned_by_id' => 1,

    /**
     * Вебхук портала dreamteamcompany для создания лидов
     */
    'portal_webhooks' => [
        'dreamteamcompany' => 'https://bitrix.dreamteamcompany.ru/rest/1/wwse63cz9w29662p/'
    ],

    /**
     * ID чата "Ошибки по рекламе" для уведомлений о необработанных файлах
     **/
    'error_chat_id' => 'chat69697', // ID чата "Ошибки по рекламе"

    // ============================================
    // Параметры обработки файлов
    // ============================================
    
    // Максимальное количество файлов для обработки за один запуск
    'max_files_per_run' => 50, // Используется в lead_processor, queue_manager, data_type_detector
    
    // Максимальное время выполнения цикла обработки (секунды)
    'max_execution_time' => 25, // Лимит времени на обработку файлов
    
    // Таймаут устаревших блокировок (секунды)
    'lock_timeout' => 300, // 5 минут - если блокировка старше, она удаляется

    // ============================================
    // Параметры повторов запросов к Bitrix24 API
    // ============================================
    
    // Количество попыток повтора при ошибке
    'max_retries' => 3,
    
    // Задержка между попытками (микросекунды)
    'retry_delay' => 1000000, // 1 секунда

    // ============================================
    // HTTP таймауты
    // ============================================
    
    // Таймаут для основных запросов к Bitrix24 (секунды)
    'curl_timeout' => 30,
    
    // Таймаут для быстрых запросов (секунды)
    'curl_timeout_short' => 10,

    // ============================================
    // Параметры создания файлов в очереди
    // ============================================
    
    // Максимальное количество попыток создания уникального имени файла
    'max_file_creation_attempts' => 5,
    
    // Задержка между попытками создания файла (микросекунды)
    'file_creation_delay' => 1000, // 1 миллисекунда

    // ============================================
    // Параметры уведомлений
    // ============================================
    
    // Порог количества файлов в RAW для отправки уведомления
    'raw_files_notification_threshold' => 10,
    
    // Интервал между уведомлениями (секунды)
    'notification_interval' => 300, // 5 минут

    // ============================================
    // Параметры очистки старых файлов
    // ============================================
    
    // Максимальное количество файлов для очистки
    'cleanup_max_files' => 200,
    
    // Минимальный интервал между очистками (секунды)
    'cleanup_min_interval' => 300, // 5 минут

    // ============================================
    // Параметры базы данных (опционально)
    // ============================================
    
    // Таймаут подключения к БД (секунды)
    'db_connect_timeout' => 5,
    
    // Таймаут чтения из БД (секунды)
    'db_read_timeout' => 5,
];
